# Core dependencies (required)
numpy>=1.24.0
scipy>=1.10.0

# macOS system interaction (required for desktop embodiment)
pyobjc>=9.0
pyobjc-framework-Quartz>=9.0
pyobjc-framework-Cocoa>=9.0

# Data processing (for connectome extraction scripts)
pandas>=2.0.0
pyarrow>=12.0.0

# Optional: visualization (for experiments and analysis)
# matplotlib>=3.7.0
# seaborn>=0.12.0

# Optional: Ollama integration (for thought generator / voice system)
# requests>=2.28.0

# Note: This project requires Python 3.10+ on macOS 12+
# The desktop embodiment features (mouse tracking, window detection,
# volume control) are macOS-only and will not work on Linux/Windows.
